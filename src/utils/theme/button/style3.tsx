import { theme } from "@styles/theme"

export const style3 = {
    defaultBgColor: theme.color.B300,
    defaultTtColor: theme.color.N0,
    disabledBgColor: theme.color.N30,
    disabledTtColor: theme.color.N80,
    activedBgColor: theme.color.B300,
    activedTtColor: theme.color.N0,
    width: "100%",
    padding: "0.8rem 1.5rem"
}