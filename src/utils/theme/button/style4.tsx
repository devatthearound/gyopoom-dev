import { theme } from "@styles/theme"

export const style4 = {
    defaultBgColor: theme.color.N30,
    defaultTtColor: theme.color.BB800,
    disabledBgColor: theme.color.N30,
    disabledTtColor: theme.color.N80,
    activedBgColor: theme.color.N30,
    activedTtColor: theme.color.BB800,
    width: "fit-content",
    padding: "1.5rem 1.9rem"
}