import { theme } from "@styles/theme"

export const style7 = {
    defaultBgColor: theme.color.N40,
    defaultTtColor: theme.color.N200,
    disabledBgColor: theme.color.N40,
    disabledTtColor: theme.color.N200,
    activedBgColor: theme.color.N40,
    activedTtColor: theme.color.N200,
    width: "fit-content",
    padding: "0.7rem 1.2rem"
}